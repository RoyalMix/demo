const verificationRoutes = require("./routes/verification");
app.use("/api/verify", verificationRoutes);
const verificationRoutes = require("./routes/verification");
app.use("/api/verify", verificationRoutes);
